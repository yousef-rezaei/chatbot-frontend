# Backend base URL (no trailing slash).
# In standalone dev: read at build/serve time by Vite.
# In widget mode: ignored — API host is derived from the widget script's own src.
VITE_API_URL=http://localhost:8000
